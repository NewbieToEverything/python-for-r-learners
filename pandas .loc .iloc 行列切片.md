# pandas DataFrame 的行列切片

> 内容整理自 Python 学习笔记中的 DeepSeek 分享页。重复回答和网页占位符已清理。

## `df[0:100]` 是什么

对 DataFrame 使用整数切片时，通常表示行切片，并且遵循 Python 的左闭右开规则：

```python
import pandas as pd

df = pd.DataFrame({"A": range(200)})

print(df[0:100].shape)  # 前 100 行，即第 0 到 99 行
```

`df[0:100,]` 和 `df[0:100, :]` 不是 pandas 的标准写法，逗号会导致错误。推荐明确使用：

```python
df.iloc[0:100]       # 按位置选择前 100 行
df.iloc[0:100, :]    # 前 100 行，所有列
```

## `[]` 的基本行为

DataFrame 直接使用 `[]` 时：

- 传入单个字符串：选择一列，返回 Series。
- 传入字符串列表：选择多列，返回 DataFrame。
- 传入切片：选择行。
- 传入布尔数组：筛选行。

```python
df["A"]              # 单列
df[["A", "B"]]      # 多列
df[0:3]              # 行切片
df[df["A"] > 5]      # 条件筛选行
```

## 列选择

### 按列名选择

```python
df["A"]
df[["A", "C"]]
```

注意：`df[["A"]]` 返回 DataFrame，而 `df["A"]` 返回 Series。

### 使用 `.loc[]`

`.loc` 基于标签选择，行和列都可以同时指定：

```python
df.loc[:, "A"]          # 所有行，A 列
df.loc[:, ["A", "B"]]   # 所有行，A、B 列
df.loc[:, "A":"C"]     # A 到 C，包含两端
```

标签切片通常包含结束标签。

### 使用 `.iloc[]`

`.iloc` 基于整数位置选择：

```python
df.iloc[:, 0]       # 第 0 列
df.iloc[:, [0, 2]]   # 第 0、2 列
df.iloc[:, 0:3]     # 第 0 到 2 列
```

`.iloc` 的切片遵循左闭右开规则。

## 同时选择行和列

```python
df.iloc[0:5, 0:2]
df.loc["row_0":"row_4", "A":"C"]
```

第一种按位置选择，第二种按标签选择。

## `.loc` 和 `.iloc` 的区别

| 写法 | 索引依据 | 结束边界 |
| :--- | :--- | :--- |
| `.iloc[0:3, 1:4]` | 整数位置 | 通常不包含结束位置 |
| `.loc[0:3, "A":"C"]` | 标签 | 标签切片通常包含结束标签 |

如果索引是非连续整数，`df[0:100]` 可能产生令人困惑的行为。因此，想按位置选行时应优先使用 `.iloc`，想按标签选行时使用 `.loc`。

## 完整示例

```python
import pandas as pd


df = pd.DataFrame(
    {
        "姓名": ["张三", "李四", "王五", "赵六", "钱七"],
        "年龄": [25, 30, 35, 28, 32],
        "性别": ["男", "女", "男", "女", "男"],
        "工资": [5000, 6000, 7000, 5500, 8000],
        "城市": ["北京", "上海", "广州", "深圳", "杭州"],
    }
)

basic_info = df[["姓名", "年龄", "性别"]]
salary_info = df.loc[:, "姓名":"工资"]
first_three_columns = df.iloc[:, :3]
young_employees = df.loc[df["年龄"] < 30, ["姓名", "年龄", "城市"]]
selected = df.iloc[[0, 2, 4], [1, 3]]
```

## 常见错误

```python
# 错误：DataFrame 不支持这种 NumPy 风格写法
# df[0:3, "A":"C"]
# df[:, 0:2]

# 正确
result = df.loc[df.index[0:3], "A":"C"]
result = df.iloc[0:3, 0:2]
```

列名切片应使用 `.loc`：

```python
# 不推荐，可能被解释为行标签切片
# df["A":"C"]

# 推荐
df.loc[:, "A":"C"]
```

## 记忆规则

1. `df[0:3]`：直接切片通常是行切片。
2. `df["A"]`：选择单列。
3. `df[["A", "B"]]`：选择多列。
4. `df.iloc[...]`：按整数位置选择。
5. `df.loc[...]`：按标签选择。
6. 需要同时选择行和列时，使用 `.loc` 或 `.iloc`。
